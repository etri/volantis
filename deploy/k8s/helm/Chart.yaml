apiVersion: v2
name: volantis
description: Volantis — a cloud-native 5G/6G mobile core for Kubernetes
type: application

# The chart's own version. Bumped when the chart changes.
version: 0.1.0

# The image tag the defaults pull. Overridden per install with
# `--set image.tag`, which is what deploy/k8s/k8s-deploy.sh does.
appVersion: "v0.1.1"

keywords:
  - 5g
  - 6g
  - mobile-core
  - service-mesh
home: https://github.com/etri/volantis
sources:
  - https://github.com/etri/volantis
